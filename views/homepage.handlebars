{{#each images as |image| }}
<div class="main">
    <div class="homepage">
        <div id="body">
            <div class="gal-img">
                <h4>{{image_name}}</h4>
                <div class="images">
                    <a class="link" href="/images/{{id}}"><img class="hp-imgs" src="/images/{{hosted_url}}"
                            alt="{{description}}"></a>
                    <p>{{description}}</p>
                </div>
            </div>


        </div>

    </div>
</div>
{{/each}}
<form action="/upload" enctype="multipart/form-data" method="post">

    <div class="form-group">
        <label for="upload-image-file"></label>
        <input type="file" class="form-control-file" name="upload" id="upload" placeholder="upload-image-file"
            aria-describedby="fileHelpId">
        <small id="fileHelpId" class="form-text text-muted">Please choose an image.</small>
    </div>

    <button type="submit" class="btn btn-primary" value="Upload">Upload</button>

</form>