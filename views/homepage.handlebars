{{#each images as |image| }}
<div class="main">
    <div class="homepage">

        <div id="body">
            <h1>{{image_name}}</h1>
            <img src="/images/{{hosted_url}}" alt="{{description}}}">
        </div>

    </div>
</div>
{{/each}}