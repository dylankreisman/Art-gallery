{{#each images as |image| }}
<div class="main">
    <div class="homepage">

        <div id="body">
            <h1>{{image_name}}</h1>
            <img src="/images/{{hosted_url}}" alt="image here">
            {{!-- <script type="text/x-handlebars-template">
                <img  src="{{hosted_url}}">
            </script> --}}
        </div>

    </div>
</div>
{{/each}}